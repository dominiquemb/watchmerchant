h2 Key Performance Indicators
.row
    each kpi in kpis
        .col.sm-6.col-md-4.col-lg-3
            .card.border.text-center.bg-light.mb-3
                .card-header
                    span= kpi.title
                .card-body
                    h1.card-title= kpi.value
                    if kpi.text
                        p.card-text= kpi.text
        